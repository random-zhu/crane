# Historical Kubernetes 1.13 manifests

These CRDs are retained only for historical reference and are excluded from the
supported build, installation, and compatibility test paths. Do not install
them on current clusters.

The actively validated Kubernetes range is 1.34-1.36. Use `deploy/manifests`
and the current binaries for that range.
