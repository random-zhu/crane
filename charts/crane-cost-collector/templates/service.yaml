apiVersion: v1
kind: Service
metadata:
  name: {{ include "crane-cost-collector.fullname" . }}
  labels:
    {{- include "crane-cost-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "crane-cost-collector.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
